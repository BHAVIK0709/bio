# Marriage Biodata (React)
- Mobile-first, bilingual (English/Gujarati), print-friendly.
- Edit details in `src/App.jsx` (the `PROFILE` object).

## Setup
npm install
npm start

## Deploy to GitHub Pages
npm run deploy
